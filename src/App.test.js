import React from "react";
import { render } from "@testing-library/react";

import { ThemeProvider } from "@material-ui/core";
import App from "./App";

test("renders App with ThemeProvider", () => {
  // it should render App with Provider child
  // ThemeProvider should contain mock theme
});

// I unfortunately have not been exposed to a lot of unit testing.
// From the reading I have done, there is a lot of reward in writing coverage
// for component functionality. Would love to chat more about how important
// unit testing is to this company!
